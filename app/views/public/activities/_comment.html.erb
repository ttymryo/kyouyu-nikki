<%= activity.subject.user.name %>さんがコメントしました<span class="ml-auto"><%= activity.created_at.strftime('%Y/%m/%d %H:%M') %></span>
<div class="w-100"></div>
<%= activity.subject.diary.body.truncate(30) %>