<%= @user.name %>
<%= @user.name_id %>
<%= link_to 'プロフィール設定', edit_user_path(@user.name_id) %>
<% @user.diaries.each do |diary| %>
  <div>
    <%= diary.user.name %>
    <%= link_to diary.body, user_diary_path(diary.user.name_id, diary) %>
  </div>
<% end %>